module HomeHelper
	

	# def bookings
 #    @bookings = Booking.all.order('created_at DESC')
 #    @booking = Booking.new
 #    @booked_events = []

 #    Booking.all.each do |booking|
 #      if current_user.booked_events.include?(booking.user_id) || current_user.id == booking.user_id
 #        @booked_events.push(booking)
 #      end
 #    end
 #    @sorted_booked_events = @booked_events.sort_by(&:created_at).reverse

  end

 